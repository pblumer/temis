{
  "name": "temis-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Temis own DMN modeler frontend (ADR-0016) — Vite + TypeScript scaffold (WP-60).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "typescript": "~7.0.2",
    "vite": "~8.1.4"
  },
  "dependencies": {
    "@dagrejs/dagre": "^3.0.0",
    "diagram-js": "^15.20.1",
    "diagram-js-direct-editing": "^3.4.0"
  }
}
