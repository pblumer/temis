// Package drg resolves the Decision Requirements Graph: it builds the
// dependency DAG from information requirements, detects cycles and produces a
// topological evaluation plan so that decisions execute in the correct order.
//
// This package is currently a scaffold (WP-01); graph evaluation starts at WP-28.
package drg
