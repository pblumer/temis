// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: dmn/v1/engine.proto

// Package dmn.v1 is the gRPC contract for the Temis DMN engine (WP-33,
// docs/40-api-contract.md §3). It mirrors the HTTP service: compile a DMN model
// once, then evaluate a named decision against an input context — statelessly
// (send the XML) or against a previously compiled model (send its id).
package dmnv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	dmnv1 "github.com/pblumer/temis/internal/gen/dmnv1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// DmnEngineName is the fully-qualified name of the DmnEngine service.
	DmnEngineName = "dmn.v1.DmnEngine"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// DmnEngineCompileProcedure is the fully-qualified name of the DmnEngine's Compile RPC.
	DmnEngineCompileProcedure = "/dmn.v1.DmnEngine/Compile"
	// DmnEngineEvaluateProcedure is the fully-qualified name of the DmnEngine's Evaluate RPC.
	DmnEngineEvaluateProcedure = "/dmn.v1.DmnEngine/Evaluate"
	// DmnEngineEvaluateBatchProcedure is the fully-qualified name of the DmnEngine's EvaluateBatch RPC.
	DmnEngineEvaluateBatchProcedure = "/dmn.v1.DmnEngine/EvaluateBatch"
)

// DmnEngineClient is a client for the dmn.v1.DmnEngine service.
type DmnEngineClient interface {
	// Compile decodes and compiles a DMN XML document, caches it and returns its
	// content-addressed id plus the index and any per-decision diagnostics.
	Compile(context.Context, *connect.Request[dmnv1.CompileRequest]) (*connect.Response[dmnv1.CompileResponse], error)
	// Evaluate runs one decision and returns its outputs (and the results of the
	// decisions it required). The model is identified by id or supplied inline.
	Evaluate(context.Context, *connect.Request[dmnv1.EvaluateRequest]) (*connect.Response[dmnv1.EvaluateResponse], error)
	// EvaluateBatch streams evaluations: each request on the stream yields one
	// response, in order, so a client can pipeline many decisions over a single
	// connection (AK WP-33: streaming for batch).
	EvaluateBatch(context.Context) *connect.BidiStreamForClient[dmnv1.EvaluateRequest, dmnv1.EvaluateResponse]
}

// NewDmnEngineClient constructs a client for the dmn.v1.DmnEngine service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewDmnEngineClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) DmnEngineClient {
	baseURL = strings.TrimRight(baseURL, "/")
	dmnEngineMethods := dmnv1.File_dmn_v1_engine_proto.Services().ByName("DmnEngine").Methods()
	return &dmnEngineClient{
		compile: connect.NewClient[dmnv1.CompileRequest, dmnv1.CompileResponse](
			httpClient,
			baseURL+DmnEngineCompileProcedure,
			connect.WithSchema(dmnEngineMethods.ByName("Compile")),
			connect.WithClientOptions(opts...),
		),
		evaluate: connect.NewClient[dmnv1.EvaluateRequest, dmnv1.EvaluateResponse](
			httpClient,
			baseURL+DmnEngineEvaluateProcedure,
			connect.WithSchema(dmnEngineMethods.ByName("Evaluate")),
			connect.WithClientOptions(opts...),
		),
		evaluateBatch: connect.NewClient[dmnv1.EvaluateRequest, dmnv1.EvaluateResponse](
			httpClient,
			baseURL+DmnEngineEvaluateBatchProcedure,
			connect.WithSchema(dmnEngineMethods.ByName("EvaluateBatch")),
			connect.WithClientOptions(opts...),
		),
	}
}

// dmnEngineClient implements DmnEngineClient.
type dmnEngineClient struct {
	compile       *connect.Client[dmnv1.CompileRequest, dmnv1.CompileResponse]
	evaluate      *connect.Client[dmnv1.EvaluateRequest, dmnv1.EvaluateResponse]
	evaluateBatch *connect.Client[dmnv1.EvaluateRequest, dmnv1.EvaluateResponse]
}

// Compile calls dmn.v1.DmnEngine.Compile.
func (c *dmnEngineClient) Compile(ctx context.Context, req *connect.Request[dmnv1.CompileRequest]) (*connect.Response[dmnv1.CompileResponse], error) {
	return c.compile.CallUnary(ctx, req)
}

// Evaluate calls dmn.v1.DmnEngine.Evaluate.
func (c *dmnEngineClient) Evaluate(ctx context.Context, req *connect.Request[dmnv1.EvaluateRequest]) (*connect.Response[dmnv1.EvaluateResponse], error) {
	return c.evaluate.CallUnary(ctx, req)
}

// EvaluateBatch calls dmn.v1.DmnEngine.EvaluateBatch.
func (c *dmnEngineClient) EvaluateBatch(ctx context.Context) *connect.BidiStreamForClient[dmnv1.EvaluateRequest, dmnv1.EvaluateResponse] {
	return c.evaluateBatch.CallBidiStream(ctx)
}

// DmnEngineHandler is an implementation of the dmn.v1.DmnEngine service.
type DmnEngineHandler interface {
	// Compile decodes and compiles a DMN XML document, caches it and returns its
	// content-addressed id plus the index and any per-decision diagnostics.
	Compile(context.Context, *connect.Request[dmnv1.CompileRequest]) (*connect.Response[dmnv1.CompileResponse], error)
	// Evaluate runs one decision and returns its outputs (and the results of the
	// decisions it required). The model is identified by id or supplied inline.
	Evaluate(context.Context, *connect.Request[dmnv1.EvaluateRequest]) (*connect.Response[dmnv1.EvaluateResponse], error)
	// EvaluateBatch streams evaluations: each request on the stream yields one
	// response, in order, so a client can pipeline many decisions over a single
	// connection (AK WP-33: streaming for batch).
	EvaluateBatch(context.Context, *connect.BidiStream[dmnv1.EvaluateRequest, dmnv1.EvaluateResponse]) error
}

// NewDmnEngineHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewDmnEngineHandler(svc DmnEngineHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	dmnEngineMethods := dmnv1.File_dmn_v1_engine_proto.Services().ByName("DmnEngine").Methods()
	dmnEngineCompileHandler := connect.NewUnaryHandler(
		DmnEngineCompileProcedure,
		svc.Compile,
		connect.WithSchema(dmnEngineMethods.ByName("Compile")),
		connect.WithHandlerOptions(opts...),
	)
	dmnEngineEvaluateHandler := connect.NewUnaryHandler(
		DmnEngineEvaluateProcedure,
		svc.Evaluate,
		connect.WithSchema(dmnEngineMethods.ByName("Evaluate")),
		connect.WithHandlerOptions(opts...),
	)
	dmnEngineEvaluateBatchHandler := connect.NewBidiStreamHandler(
		DmnEngineEvaluateBatchProcedure,
		svc.EvaluateBatch,
		connect.WithSchema(dmnEngineMethods.ByName("EvaluateBatch")),
		connect.WithHandlerOptions(opts...),
	)
	return "/dmn.v1.DmnEngine/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case DmnEngineCompileProcedure:
			dmnEngineCompileHandler.ServeHTTP(w, r)
		case DmnEngineEvaluateProcedure:
			dmnEngineEvaluateHandler.ServeHTTP(w, r)
		case DmnEngineEvaluateBatchProcedure:
			dmnEngineEvaluateBatchHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedDmnEngineHandler returns CodeUnimplemented from all methods.
type UnimplementedDmnEngineHandler struct{}

func (UnimplementedDmnEngineHandler) Compile(context.Context, *connect.Request[dmnv1.CompileRequest]) (*connect.Response[dmnv1.CompileResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("dmn.v1.DmnEngine.Compile is not implemented"))
}

func (UnimplementedDmnEngineHandler) Evaluate(context.Context, *connect.Request[dmnv1.EvaluateRequest]) (*connect.Response[dmnv1.EvaluateResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("dmn.v1.DmnEngine.Evaluate is not implemented"))
}

func (UnimplementedDmnEngineHandler) EvaluateBatch(context.Context, *connect.BidiStream[dmnv1.EvaluateRequest, dmnv1.EvaluateResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("dmn.v1.DmnEngine.EvaluateBatch is not implemented"))
}
